import { dayBuilder, W } from '../lib.js';
const q = dayBuilder(14);

// Checkpoint 2: no concept cards, no hints, no genius thread. Every question stands alone, every
// number is new (never a value reused from days 8-13), and the working is shown in every explain.
export default {
  title: 'Checkpoint 2 — arithmetic, data, counting (one day left before uni)',
  emoji: '🏆',
  kind: 'checkpoint',
  strands: ['H', 'S', 'A', 'M', 'E'],
  summary: 'Week 2 under test, with fresh numbers throughout: signed overflow and the carry-out trap, wrap against saturation, product and accumulator widths, fixed point and rounding policy, priority encoders, evidence and boundary tests; bytes, endianness and `struct`, parser precedence and reason codes, exceptions, two-sum, prefix sums and big-O; the product rule, factorials, combinations, repeated letters, stars and bars, pigeonhole; integrals, complex numbers, RC, first-order ODEs, RMS and superposition. Two build exercises: a signed accumulator with an overflow counter, and a linear-time count of subarrays divisible by m. **Day 15 is the last day before university starts**, so this is the last full checkpoint of the pre-term run.',
  takeaway: 'Two weeks of foundations checked. Ledger entry: what false assumption did I correct this week, and what is the highest-value next task? (Handbook Sunday questions.) Tomorrow is the last day before Manchester begins.',
  steps: [
    // =====================================================================================================
    // HARDWARE
    // =====================================================================================================
    q.num('H', 'Two signed 8-bit values 95 and 40 (range −128 … 127) are added by an 8-bit adder that **wraps**. What signed value appears in the result?', -121,
      'The true sum is 135, which is above the maximum 127. The adder keeps the low 8 bits, so the answer is 135 − 256 = −121. Pattern 10000111, read as signed: −128 + 7 = −121. Two positives producing a negative is exactly signed overflow.'),
    q.num('H', 'Two signed 8-bit values 95 and 40 (range −128 … 127) are added by an adder that **saturates** instead of wrapping. What value appears in the result?', 127,
      'The true sum 135 is above the maximum, so it is clamped to 127. Saturation sticks at the edge; wrapping would teleport the value to −121 at the far end of the range.'),
    q.num('H', 'Two signed 8-bit values −90 and −55 (range −128 … 127) are added by an 8-bit adder that wraps. What signed value appears in the result?', 111,
      'The true sum is −145, which is below the minimum −128. Add 256 to bring it into range: −145 + 256 = 111. Two negatives produced a positive, which is overflow on the negative side.'),
    q.tf('H', 'An 8-bit adder is given the patterns 11111011 and 11111101, which as signed values are −5 and −3. The carry-out is 1, so the addition overflowed.', false,
      'The true sum is −8, and −8 is comfortably inside −128 … 127, so nothing overflowed. Unsigned, 251 + 253 = 504, which needs a ninth bit, so the carry-out is 1. Carry-out answers the **unsigned** question; signed overflow is "same-sign operands, opposite-sign result", and here the result is negative like both operands.'),
    q.mc('H', 'A 4-bit signed value 1011 (which is −5) is sign-extended to 8 bits. What is the 8-bit pattern?', ['11111011', '00001011', '10110000', '10001011'], 0,
      'Sign extension copies the top bit into every new upper bit. The top bit is 1, so the four new bits are all 1: 11111011. Check the value: 256 − 251 = 5, so the pattern is −5, unchanged. Padding with zeros would give 11 instead.', { grid: true }),
    q.mc('H', 'Two 8-bit patterns 10000001 and 00000010 are compared as **two\'s complement signed** values. Which statement is true?', ['10000001 is smaller: as signed it is −127, and −127 < 2', '10000001 is larger: 129 > 2', 'They are equal', 'Signed comparison of 8-bit patterns is undefined'], 0,
      '10000001 = −128 + 1 = −127. An unsigned comparator on the same wires would report 129 > 2, the opposite answer. The comparator must match the signedness written in the contract, because the wires themselves carry no such information.'),
    q.num('H', 'An unsigned 12-bit value is multiplied by an unsigned 18-bit value. What is the minimum result width, in bits, that always holds the product?', 30,
      'm + n = 12 + 18 = 30. The largest product is (2¹² − 1)(2¹⁸ − 1) = 2³⁰ − 2¹⁸ − 2¹² + 1, just under 2³⁰, so 30 bits fit and 29 do not.'),
    q.num('H', 'Exam-style: a signed 6-bit value (range −32 … 31) is multiplied by another signed 6-bit value. State the minimum result width, in bits, that always holds the product.', 12,
      'The extreme product is not 31 × 31 = 961 but (−32) × (−32) = 1024, because the negative end of a signed range reaches one step further than the positive end. A signed 11-bit value tops out at 2¹⁰ − 1 = 1023, one short, so 12 bits are needed. Signed m × n also needs m + n; only the corner that hurts you changes.'),
    q.num('H', 'A price is stored as the integer 3456 with an agreed scale of 128. What is the real price?', 27,
      '3456/128 = 27.0. The stored integer means nothing on its own; the scale lives in the interface contract, and if the two ends disagree about it the number is wrong by a factor of 128 with nothing to complain.'),
    q.mc('H', 'Two values are each stored in Q4.12 format, so each is an integer at a scale of 4096. Their stored integers are multiplied together. What is the scale of that raw product?', ['16,777,216, because 4096 × 4096 = 2²⁴', '4096, because both inputs used 4096', '8192, because the scales add', '2048, because multiplication halves the scale'], 0,
      'Values and scales both multiply: (a·S)(b·S) = ab·S². Two Q4.12 numbers give a Q8.24 product, 32 bits wide (Day 10\'s m + n rule), and one shift right by 12 brings it back to Q4.12 — after the full 32-bit value has existed.', { grid: true }),
    q.num('H', 'A fixed-point multiply produces the exact value −800 at a scale of 64, and it is rescaled to whole units by an arithmetic shift right of 6 bits. What integer results?', -13,
      '−800/64 = −12.5 exactly. An arithmetic shift right rounds towards −∞, so the answer is −13. C\'s integer division truncates towards zero and would give −12, and round-half-to-even would also give −12 (−12 is the even neighbour). Three defensible answers from one expression, which is why the rounding policy belongs in the contract.'),
    q.mc('H', 'An 8-line priority encoder in which the **highest-numbered** request wins is given a pattern with req2 and req6 both asserted and every other line low. What does it output?', ['index 6, valid 1', 'index 2, valid 1', 'index 8, valid 1', 'valid 0, because two lines conflict'], 0,
      'Fixed priority resolves the clash by rank, not by negotiation: the highest asserted index takes the output, so the answer is 6 (binary 110) with valid 1. req2 is invisible until req6 drops, which is the design working as specified — and also its weakness, since a line that never stops asking starves everything below it.'),
    q.num('H', 'How many distinct input pairs does an exhaustive test of a 6-bit + 6-bit adder require (no carry-in)?', 4096,
      '64 values on each input: 64 × 64 = 2¹² = 4096. A simulator does that in milliseconds, so at this width there is no reason to sample randomly — exhaustion is a complete proof of the function.'),
    q.mc('H', 'Exam-style: a risk check must reject an order when its notional value is **strictly greater than 500**, but the design was written as "greater than or equal to 500". Which single test input exposes the mistake?', ['notional = 500', 'notional = 499', 'notional = 501', 'notional = 0'], 0,
      'At 499 both versions accept and at 501 both reject; only the value exactly at the limit behaves differently. That is why boundary sets are L − 1, L, L + 1, and why uniform random inputs over a wide range almost never find an off-by-one.', { grid: true }),
    q.num('H', 'Interview: a quantity of 200 is multiplied by a price of 30,000,000 and the product is kept in a 32-bit unsigned register before any comparison. What value does the register hold?', 1705032704,
      'The true product is 6,000,000,000. A 32-bit register keeps the remainder modulo 2³² = 4,294,967,296: 6,000,000,000 − 4,294,967,296 = 1,705,032,704. A limit of 2,000,000,000 would now accept an order worth three times the limit. The fix is ordering, not a wider variable at the end: the full 48-bit product must exist before anything is compared.'),
    q.mc('H', 'Interview: a colleague drives an 8-to-1 data mux directly from a priority encoder\'s index output and leaves the encoder\'s `valid` output unconnected. What is the first failure you would predict?', ['With no request asserted the index still reads 0, so the mux keeps forwarding channel 0 as if it had been selected', 'The mux outputs all zeros, which is safe', 'Synthesis refuses to build the design', 'The index bits float and the chip draws excess current'], 0,
      'Three index bits have eight patterns and all eight already name a real line, so "nobody asked" can only be carried by a separate valid bit. Dropping it turns no request into "channel 0 asked", and nothing downstream can tell the difference. Gate the mux enable with valid.'),
    q.code('H', 'Build: write `solve(w, xs)`, a cycle-level model of two **signed w-bit accumulators** that both read one shared list `xs`, one addition per cycle, both starting at 0. The first accumulator **wraps** (keep the low w bits and read them as signed); the second **saturates** (clamp to −2^(w−1) … 2^(w−1) − 1). Also count, for the **wrapping** accumulator, how many additions had a true sum outside the signed range — that is its signed-overflow count. Return `[wrap_final, sat_final, overflow_count]`. It must work at w = 1 and w = 17, and a 50,000-input stream must fit the time budget.', {
      fn: 'solve',
      starter: 'def solve(w, xs):\n    lo = -(2 ** (w - 1))\n    hi = 2 ** (w - 1) - 1\n    acc_w = 0\n    acc_s = 0\n    overflows = 0\n    for x in xs:\n        t = acc_w + x          # the true sum for the wrapping accumulator\n        # count it if t is outside lo..hi, then wrap t into lo..hi\n        u = acc_s + x          # the true sum for the saturating accumulator\n        # clamp u into lo..hi\n        pass\n    return [acc_w, acc_s, overflows]\n',
      tests: [
        { args: [8, [70, 70, -30]], expect: [110, 97, 2], name: 'wrap 70 → −116 → 110; saturate 70 → 127 → 97; two overflows' },
        { args: [8, []], expect: [0, 0, 0], name: 'no inputs at all' },
        { args: [8, [100, 27]], expect: [127, 127, 0], name: 'landing exactly on the maximum is not an overflow' },
        { args: [8, [-128, -1]], expect: [127, -128, 1], name: 'one step below the minimum wraps to the top' },
        { args: [4, [7, 1]], expect: [-8, 7, 1], name: 'width 4: the largest value plus one becomes the smallest' },
        { args: [1, [1, 1, -1]], expect: [-1, -1, 1], name: 'width 1: the range is just −1 … 0' },
        { args: [17, [65535, 1]], expect: [-65536, 65535, 1], name: 'width 17: the maximum is 65535' },
        { args: [8, [40, -90, 40]], expect: [-10, -10, 0], name: 'a mixed-sign walk that never leaves the range' },
      ],
      gen: 'def gen():\n    for _ in range(50):\n        w = random.choice([1, 2, 3, 4, 8, 12, 17])\n        n = random.randint(0, 25)\n        big = 2 ** w\n        yield [w, [random.randint(-big, big) for _ in range(n)]]',
      refCode: 'def ref(w, xs):\n    lo, hi = -(1 << (w - 1)), (1 << (w - 1)) - 1\n    aw, asat, n = 0, 0, 0\n    for x in xs:\n        t = aw + x\n        if t < lo or t > hi:\n            n += 1\n        aw = ((t - lo) % (1 << w)) + lo\n        asat = max(lo, min(hi, asat + x))\n    return [aw, asat, n]',
      speed: { gen: 'def gen():\n    return [8, [random.randint(-200, 200) for _ in range(50000)]]', budgetMs: 1500, label: '50,000 inputs, width 8' },
      solution: 'def solve(w, xs):\n    lo = -(2 ** (w - 1))\n    hi = 2 ** (w - 1) - 1\n    span = 2 ** w\n    acc_w = 0\n    acc_s = 0\n    overflows = 0\n    for x in xs:\n        t = acc_w + x\n        if t < lo or t > hi:\n            overflows += 1\n        acc_w = ((t - lo) % span) + lo\n        u = acc_s + x\n        if u > hi:\n            u = hi\n        elif u < lo:\n            u = lo\n        acc_s = u\n    return [acc_w, acc_s, overflows]'
    }, 'Three ideas from week 2 in one loop. Wrapping is "reduce modulo 2^w back into the signed window": (t − lo) % 2^w + lo shifts the window so Python\'s always-non-negative remainder lands in lo … hi. Saturating is max(lo, min(hi, u)) — the two comparators and the mux of the real hardware. The overflow count is Day 8\'s detection rule stated on the true sum, and it is counted on the wrapping accumulator because the saturating one has already thrown the excess away. Notice the two accumulators diverge permanently after the first clamp, which is the whole reason the policy is part of the interface contract rather than an implementation detail. Width 1 is the sharp edge: lo = −1, hi = 0, so 0 + 1 already leaves the range.'),

    // =====================================================================================================
    // CODE
    // =====================================================================================================
    q.num('S', 'The two bytes `03 09` arrive on a network link and are read as a 16-bit **big-endian** unsigned value. What number are they?', 777,
      'Big-endian puts the most significant byte first: 3 × 256 + 9 = 768 + 9 = 777 (0x0309).'),
    q.num('S', 'The two bytes `03 09` are read as a 16-bit **little-endian** unsigned value. What number is that?', 2307,
      'Little-endian puts the least significant byte first, so the second byte carries the 256s: 9 × 256 + 3 = 2304 + 3 = 2307. Identical bytes, a different number — only the specification decides which reading is right.'),
    q.num('S', 'The two bytes `FF F0` are decoded as a big-endian **signed** 16-bit value. What value results?', -16,
      'Assemble first: 255 × 256 + 240 = 65,520. Then apply two\'s complement at 16 bits: the top bit is set, so subtract 2¹⁶: 65,520 − 65,536 = −16. Decoded as unsigned (`>H`) the same bytes would be 65,520.'),
    q.mc('S', "In Python's `struct`, which format string reads a **big-endian signed 16-bit** integer?", ["'>h'", "'>H'", "'<h'", "'h'"], 0,
      "'>' means big-endian, lower-case 'h' means signed 2 bytes. Upper-case 'H' is unsigned, '<' is little-endian, and a bare 'h' uses the running machine's native order and padding, which is wrong for a format defined on the wire.", { grid: true }),
    q.mc('S', 'A binary format defines messages as exactly 16 bytes and 20 bytes arrive in one read. What should the parser do?', ['Reject with a length reason before decoding any field', 'Decode the first 16 bytes and discard the rest silently', 'Decode all 20 bytes and ignore the fields it does not recognise', 'Let the struct.error escape and be caught somewhere further up'], 0,
      'The length check is stage one for a reason: a message that is not the promised size is not a message of that format, and guessing which 16 of the 20 bytes were meant is inventing data. Refuse with a named reason so the rejection can be counted and tested.'),
    q.text('S', 'A parser for a 16-byte message checks its rules in this fixed precedence: length, version, reserved byte, opcode, side, then the quantity rules. A 16-byte message arrives with version 1, a reserved byte of 3, opcode 9 and side 4. Which reason code does it return? (Use `bad_version`, `reserved_nonzero`, `bad_opcode` or `bad_side`.)', ['reserved_nonzero'],
      'The version is legal, so the first failing rule is the reserved byte, which must be 0. Precedence is what makes the answer stable: three rules fail, but every implementation stops at the first, so a hardware design and its Python reference model agree on the reason and not merely on "rejected".', { mono: true, accept: ['^reserved[_ ]?nonzero$'] }),
    q.mc('S', 'A Python program runs `prices = {"AAPL": 190}` and then evaluates `prices["MSFT"]`. Which exception is raised?', ['KeyError', 'IndexError', 'ValueError', 'TypeError'], 0,
      'A dictionary key that is not present raises KeyError. IndexError is the list version, for a position outside 0 … len−1. ValueError would be the right type with an impossible value, such as `int("abc")`.', { grid: true }),
    q.tf('S', 'Wrapping a parser in `try: ... except: pass` is good defensive programming, because the program then never crashes.', false,
      'A bare `except:` catches everything — a misspelled variable name (NameError), a wrong-type call (TypeError), memory exhaustion, even Ctrl-C. The program stops crashing and starts silently producing nothing, and a real bug arrives disguised as a rejected message. Catch the specific exception you expect, at the place you expect it.'),
    q.mc('S', 'A Python program runs `a = [4, 5]`, then `b = a.copy()`, then `b.append(6)`. What is `a` afterwards?', ['[4, 5]', '[4, 5, 6]', '[6]', '[4, 5, 4, 5, 6]'], 0,
      '`copy()` builds a separate list holding the same items, so appending to `b` cannot touch `a`. Writing `b = a` instead would make both names refer to one list, and `a` would then read [4, 5, 6].', { grid: true }),
    q.mc('S', 'Interview: a decoder sitting on a network boundary meets an opcode it does not recognise. Which design does the handbook favour, and why?', ['Return a structured rejection carrying a reason code, so rejections can be counted per rule, tested per rule and triaged when a sender changes format', 'Return None, so the caller knows something went wrong', 'Print a warning and carry on decoding the remaining fields', 'Guess the nearest defined opcode, since unknown values are usually typos'], 0,
      'Malformed input from a network is expected traffic, not a bug in your own code, so it deserves a countable, testable value rather than an exception. None collapses every distinct cause into one bucket: you learn how many failed and never why.'),

    // =====================================================================================================
    // ALGORITHMS
    // =====================================================================================================
    q.mc('A', 'A one-pass hash-map two-sum runs on the list [7, 2, 9, 5, 4] with target 11, looking up `target − x` before inserting x. Which pair of indices does it report?', ['1 and 2', '0 and 4', '3 and 4', '0 and 1'], 0,
      'Index 0 holds 7 and needs 4, which has not been seen. Index 1 holds 2 and needs 9, not seen yet either. Index 2 holds 9 and needs 2, which was recorded at index 1 — so the answer is (1, 2). The later pair 7 + 4 exists too, but the scan reports the first pair completed.'),
    q.mc('A', 'A one-pass hash-map two-sum is run on the list [6] with target 12. What should it return?', ['Nothing found: one element cannot pair with its own position', 'The indices (0, 0)', 'The index 0 on its own', '6'], 0,
      'The requirement is two **distinct positions**. This one-element test is exactly what catches insert-before-lookup: inserting 6 first would put it in the dictionary at index 0, and the lookup for 12 − 6 = 6 would then find it and report (0, 0), one element used twice.'),
    q.tokens('A', 'The list [3, −1, 4, −2] is read left to right. Put its four running prefix sums in order.', ['3', '2', '6', '4'], ['1', '5'],
      '3, then 3 − 1 = 2, then 2 + 4 = 6, then 6 − 2 = 4. Together with the empty prefix P[0] = 0 these are every prefix of the list, and any range sum is then one subtraction: the sum of [−1, 4] is P[3] − P[1] = 6 − 3 = 3.', { mono: true }),
    q.num('A', 'How many contiguous subarrays of the list [1, 2, −1, 2] have a sum of exactly 3?', 2,
      'The prefixes are 1, 3, 2, 4 against a frequency map seeded with {0: 1}. At prefix 3 an earlier 0 is present once (the run [1, 2]); at prefix 4 an earlier 1 is present once (the run [2, −1, 2]). Total 2. Listing them by hand agrees: [1, 2] = 3 and [2, −1, 2] = 3.'),
    q.mc('A', 'Interview: a one-pass counter of subarrays summing to K keeps a frequency map of prefix sums and seeds it with `count[0] = 1` before reading anything. Why is that seed necessary, and what breaks without it?', ['The empty prefix has sum 0, so without the seed a subarray that starts at index 0 has no earlier prefix to match and every such run is missed', 'Because list indices start at zero, so the map needs an entry at 0', 'To stop a later division by zero', 'It is optional and only affects speed'], 0,
      'When the running prefix itself equals K, the qualifying subarray is the whole run from the start of the list. Finding it needs a recorded prefix of 0 from before the first element, and that is exactly what the seed is. Drop it and [3, 4] with K = 7 returns 0 instead of 1.'),
    q.num('A', 'A single pass finds the largest value in a list of 35 numbers by keeping a running best. How many comparisons does it perform?', 34,
      'The first element becomes the running best for free, and each of the remaining 34 elements is compared against it once: n − 1 = 34. No method can do better, because every element except the winner must lose at least one comparison.'),
    q.mc('A', 'Which of these growth classes grows fastest as n becomes large: O(n log n), O(n²), O(2ⁿ), O(n)?', ['O(2ⁿ)', 'O(n²)', 'O(n log n)', 'O(n)'], 0,
      'Exponential growth outruns every polynomial eventually: doubling n **squares** the work for O(2ⁿ), while it only quadruples it for O(n²). At n = 40 an O(2ⁿ) algorithm needs about 10¹² steps, which is why exhaustive search over 40 binary choices is hopeless.', { grid: true }),
    q.mc('A', 'Interview: a hash-map lookup is usually described as O(1). What is the precise claim being made, and when would the difference matter?', ['Expected constant time: keys can collide into one bucket and the lookup then walks a chain, so an untrusted party choosing the keys can drag it towards linear', 'Exactly one machine instruction, always', 'Constant time in the worst case, guaranteed', 'Constant time for any hash function whatsoever'], 0,
      'The constant-time behaviour is an average over well-spread keys. An adversary who knows the hash function can choose keys that all collide — a real denial-of-service technique, and the reason Python randomises string hashing on each run. For your own internal data the expected case is an honest answer; for attacker-supplied data it is not.'),
    q.mc('A', 'Interview: you measure an algorithm at n = 2000 (0.5 s), n = 4000 (2.0 s) and n = 8000 (8.1 s). What do you conclude, and what would you look for?', ['Each doubling multiplies the time by about four, so the growth is quadratic: look for a loop nested inside a loop, or a linear scan performed once per item', 'The growth is linear, since the times rise steadily', 'The growth is O(n log n), the usual cost of sorting', 'Three points are never enough to conclude anything'], 0,
      '2.0/0.5 = 4.0 and 8.1/2.0 = 4.05. A factor of four per doubling is the signature of n². The usual culprit is a membership test written as `x in some_list`, which is a scan; `x in some_dict` is a hash lookup, and the two lines differ by a factor of n. Read the ratio from the largest sizes, where the constants no longer dominate.'),
    q.code('A', 'Build: write `solve(arr, m)` returning the number of contiguous subarrays of `arr` whose elements sum to a multiple of `m` (with `m` at least 1). Use one pass: keep the running prefix sum **modulo m**, keep a dictionary counting how often each remainder has already been seen (seeded with `{0: 1}` for the empty prefix), and add the count of the current remainder to the answer **before** recording it. A range sum P[j] − P[i] is divisible by m exactly when P[j] and P[i] leave the same remainder. Values may be negative, the list may be empty, and a 50,000-element list must fit the time budget.', {
      fn: 'solve',
      starter: 'def solve(arr, m):\n    counts = {0: 1}   # remainder -> how many earlier prefixes left it\n    current = 0\n    answer = 0\n    for x in arr:\n        current = (current + x) % m\n        # add counts.get(current, 0) to answer, then record current\n        pass\n    return answer\n',
      tests: [
        { args: [[4, 5, 0, -2, -3, 1], 5], expect: 7, name: 'the classic mixed-sign case' },
        { args: [[], 3], expect: 0, name: 'an empty list has no subarrays' },
        { args: [[5], 5], expect: 1, name: 'a single element that is itself a multiple' },
        { args: [[5], 3], expect: 0, name: 'a single element that is not' },
        { args: [[0, 0, 0], 7], expect: 6, name: 'three zeros: every one of the n(n+1)/2 subarrays qualifies' },
        { args: [[-1, -1, -1], 3], expect: 1, name: 'negatives: only the whole list sums to −3' },
        { args: [[3, 6, 9], 3], expect: 6, name: 'every element is a multiple, so every subarray is' },
        { args: [[1, 2, 3], 1], expect: 6, name: 'm = 1: everything is divisible by 1' },
        { args: [[2, 2, 2, 2], 4], expect: 4, name: 'overlapping matches are all counted' },
      ],
      gen: 'def gen():\n    for _ in range(40):\n        n = random.randint(0, 14)\n        yield [[random.randint(-6, 6) for _ in range(n)], random.randint(1, 6)]',
      refCode: 'def ref(arr, m):\n    n = len(arr)\n    c = 0\n    for i in range(n):\n        s = 0\n        for j in range(i, n):\n            s += arr[j]\n            if s % m == 0:\n                c += 1\n    return c',
      speed: { gen: 'def gen():\n    return [[random.randint(-5, 5) for _ in range(50000)], 7]', budgetMs: 1500, label: '50,000 elements, m = 7' },
      solution: 'def solve(arr, m):\n    counts = {0: 1}\n    current = 0\n    answer = 0\n    for x in arr:\n        current = (current + x) % m\n        answer += counts.get(current, 0)\n        counts[current] = counts.get(current, 0) + 1\n    return answer'
    }, 'The whole exercise is one substitution into the prefix-sum identity: instead of asking for an earlier prefix equal to current − K, ask for an earlier prefix with the same remainder, because P[j] − P[i] is a multiple of m exactly when P[j] ≡ P[i] (mod m). Two details carry the correctness. The seed {0: 1} is the empty prefix, so a run starting at index 0 has something to match. And the lookup must come before the record: otherwise the current prefix would match itself and every position would contribute a phantom empty subarray. Python\'s `%` returns a non-negative remainder for positive m, so negative values need no special handling — in C they would, since −1 % 5 is −1 there. The map holds at most m keys, and the hidden random checks compare the answer against a quadratic brute force that tries every start and end.'),

    // =====================================================================================================
    // MATHS
    // =====================================================================================================
    q.num('M', 'How many ordered pairs of two **different** symbols can be made from 9 distinct symbols?', 72,
      '9 choices for the first slot, then 8 remain for the second: 9 × 8 = 72. AB and BA are counted separately, because the pair is ordered.'),
    q.num('M', 'Exam-style: nine runners finish a race with no dead heats. How many possible orderings are there for the gold, silver and bronze medals?', 504,
      'Only three slots are filled, so the falling product stops early: 9 × 8 × 7 = 504, which is 9!/6!. It is not 9! = 362,880, because the other six places are not being ranked, and it is not C(9, 3) = 84, because the medals are different — order matters here.'),
    q.num('M', 'How many ways are there to choose 4 items from 9 when order does not matter? (That is C(9, 4).)', 126,
      'Ordered: 9 × 8 × 7 × 6 = 3024. Each unordered set of 4 appears in 4! = 24 orders, so 3024/24 = 126. The division is legal because every set is counted exactly 24 times, never more or fewer.'),
    q.mc('M', "Pascal's rule says C(9, 4) = C(8, 3) + C(8, 4). Which argument proves it?", ['Fix one particular item: every 4-subset either contains it (choose the other 3 from the remaining 8) or does not (choose all 4 from the remaining 8), and no subset does both', 'Because 56 + 70 happens to equal 126', 'Because C(n, k) = C(n, n − k) applied twice', 'Because a row of Pascal\'s triangle sums to 2ⁿ'], 0,
      'The case split covers everything and overlaps nowhere, so the counts add: C(8, 3) = 56 and C(8, 4) = 70 give 126. The argument never used the numbers 9 and 4, so it holds in general — and it lets a machine build the whole triangle with additions only.'),
    q.num('M', 'Exam-style: how many distinct arrangements are there of the seven letters of the word SUCCESS?', 420,
      'Seven letters with S repeated 3 times and C repeated twice: 7!/(3! · 2!) = 5040/(6 × 2) = 5040/12 = 420. The division is justified because each real word comes from exactly 3! × 2! = 12 of the labelled arrangements S₁S₂S₃C₁C₂.'),
    q.num('M', 'Interview: how many binary strings of length 10 contain exactly four 1s?', 210,
      'Choosing which four of the ten positions hold the 1s **is** the string: C(10, 4) = (10 × 9 × 8 × 7)/24 = 5040/24 = 210. It is also the arrangement count 10!/(4! · 6!) — choosing a subset and arranging two kinds of identical item are the same problem.'),
    q.num('M', '7 identical jobs are distributed among 4 labelled queues, and a queue may be left empty. How many configurations are there?', 120,
      'Stars and bars: 7 stars and 3 bars make a row of 10 slots, and you choose which 3 hold bars. C(10, 3) = (10 × 9 × 8)/6 = 120. In general n identical items into k labelled bins is C(n + k − 1, k − 1).'),
    q.num('M', '7 identical jobs are distributed among 4 labelled queues with **at least one job in every queue**. How many configurations are there?', 20,
      'Pay the minimum up front: hand one job to each of the 4 queues, which spends 4 of the 7 and leaves 3 to spread with no restriction at all. Stars and bars on 3 identical items into 4 labelled bins: C(3 + 3, 3) = C(6, 3) = 20.'),
    q.num('M', '7 jobs, each with its own name, are assigned to 4 labelled queues. How many assignments are there?', 16384,
      'Each named job independently picks one of 4 queues, so 4⁷ = 16,384. The identical-jobs answer, 120, is far smaller because it records only how many landed in each queue, not which job went where. Deciding whether the items are distinguishable comes before any arithmetic.'),
    q.num('M', 'Interview: a PIN is 3 digits long, each digit from 0 to 9 and repeats allowed. How many such PINs contain **at least one 7**?', 271,
      'Count the complement. All PINs: 10³ = 1000. PINs with no 7 at all: 9³ = 729, since each digit has nine remaining choices. So 1000 − 729 = 271. Counting "at least one" directly needs three overlapping cases; the complement is one subtraction.'),
    q.num('M', 'Interview: seven people sit at a round table, and two seatings that differ only by rotating everybody one place are counted as the same. How many seatings are there?', 720,
      'Each circular seating appears once per rotation among the 7! = 5040 line-ups, so 5040/7 = 720 = 6!. Equivalently: seat one person anywhere to kill the rotation, then arrange the other six in 6! = 720 ways.'),
    q.num('M', 'Four fair six-sided dice are thrown. What is the expected value of their total?', 14,
      'One fair die has expected value (1 + 2 + 3 + 4 + 5 + 6)/6 = 3.5, and expectation adds whether or not the throws are independent: 4 × 3.5 = 14. Notice you never had to think about the 1296 possible outcomes.'),
    q.num('M', 'Items are dropped into 9 boxes in any way at all. What is the smallest number of items that **guarantees** some box holds at least 4?', 28,
      'If every box held at most 3, the total would be at most 9 × 3 = 27, so 27 items can be spread with no box reaching 4. The 28th item must push some box to 4. Generalised pigeonhole: n > m·k forces a box with k + 1, and ⌈28/9⌉ = 4 agrees.'),
    q.num('M', 'Interview: a drawer holds socks in 6 colours, mixed up in the dark. How many socks must you take out to be **certain** of a matching pair?', 7,
      'The boxes are the 6 colours. Six socks might be one of each, so 6 is not a guarantee. The 7th must repeat a colour, so the answer is 7. In general k colours need k + 1 — and note the question asks for certainty, not for a likely outcome.'),
    q.num('M', 'Interview: how many solutions in non-negative integers does x₁ + x₂ + x₃ = 12 have if every variable must also be **at most 6**?', 28,
      'Unrestricted first: stars and bars gives C(12 + 2, 2) = C(14, 2) = 91. Now remove the bad ones. If x₁ ≥ 7, substitute y = x₁ − 7 so y₁ + x₂ + x₃ = 5, giving C(7, 2) = 21 solutions, and any of the 3 variables could be the offender: 63. Two variables could not both exceed 6, since that needs 14 > 12, so nothing was subtracted twice. 91 − 63 = 28.'),

    // =====================================================================================================
    // DEGREE
    // =====================================================================================================
    q.num('E', 'Evaluate ∫₀³ (x² + 2) dx.', 15,
      'An antiderivative is F(x) = x³/3 + 2x, so the value is F(3) − F(0) = 27/3 + 6 = 9 + 6 = 15. Sanity check as a shape: the height runs from 2 to 11 over a width of 3, so the area must lie between 6 and 33 and nearer the middle. It does.'),
    q.num('E', 'The function f(x) = x⁴ is differentiated. What is f′(3)?', 108,
      'The power rule gives f′(x) = 4x³, so f′(3) = 4 × 27 = 108. Differentiation and integration undo each other: ∫ 4x³ dx = x⁴ + C.'),
    q.num('E', 'Exam-style: the current through a component is i(t) = 6t² amperes for 0 ≤ t ≤ 2 s. Calculate the total charge delivered in that time, in coulombs.', 16,
      'Charge is accumulated current: q = ∫₀² 6t² dt = [2t³]₀² = 16 C. The units confirm the shape of the idea: amps × seconds = coulombs, so an area on a current–time graph is a charge.', { unit: 'C' }),
    q.num('E', 'What is the modulus |5 + 12i|?', 13,
      '√(5² + 12²) = √(25 + 144) = √169 = 13. The modulus is just Pythagoras on the two components of the arrow.'),
    q.num('E', 'What is the angle of the complex number −5 + 12i, measured anticlockwise from the positive real axis, in degrees to 2 decimal places?', 112.62,
      'Use atan2(12, −5), not atan(12/−5). The reference angle is atan(12/5) = 67.38°, and the arrow is in the second quadrant (real part negative, imaginary part positive), so the answer is 180° − 67.38° = 112.62°. Plain atan would wrongly return −67.38°.', { tol: 0.05 }),
    q.num('E', 'Exam-style: compute (3 + 4i)/(1 + 2i) and give its **real part**, to 2 decimal places.', 2.2,
      'Multiply top and bottom by the conjugate 1 − 2i so the bottom becomes real: (3 + 4i)(1 − 2i) = 3 − 6i + 4i − 8i² = 11 − 2i, and the bottom is 1² + 2² = 5. So the answer is 2.2 − 0.4i, and the real part is 2.2.', { tol: 0.01 }),
    q.num('E', 'Exam-style: a voltage step is applied at t = 0 to a 3.3 kΩ resistor in series with an uncharged 100 nF capacitor. Find the time constant, in microseconds.', 330,
      'τ = R·C = 3300 Ω × 100 × 10⁻⁹ F = 3.3 × 10⁻⁴ s = 330 µs. The units work out because ohms are volts per amp and farads are coulombs per volt, so their product is coulombs per amp, which is seconds.', { unit: 'µs', tol: 2 }),
    q.num('E', 'Exam-style: a voltage step is applied at t = 0 to a 3.3 kΩ resistor in series with an uncharged 100 nF capacitor. How long does the capacitor voltage take to reach 80 % of its final value, in microseconds (nearest whole number)?', 531,
      'τ = RC = 330 µs, and t = −τ·ln(1 − f) = −330 × ln(0.2) = 330 × 1.6094 = 531 µs. Note the pattern: every further factor of ten in accuracy costs another 2.303 τ, so waiting longer stops helping quickly.', { unit: 'µs', tol: 4 }),
    q.num('E', 'For the differential equation dy/dt + 8y = 24, what value does y settle at once it has stopped changing?', 3,
      'Settled means dy/dt = 0, so 8y = 24 and y = 3. The steady state is b/a, and the equation pulls towards it from either side: below 3 the rate 24 − 8y is positive, above 3 it is negative. The time constant is a separate matter: τ = 1/a = 0.125 s, so a sets how fast and b sets where.'),
    q.num('E', 'Exam-style: a quantity obeys dy/dt + 4y = 12 with y(0) = 0. Find y at t = 0.25 s, to 3 decimal places.', 1.896,
      'Steady state 12/4 = 3 and decay rate 4, so y = 3(1 − e^(−4t)). At t = 0.25 s, which is exactly one time constant (τ = 1/4), y = 3(1 − e⁻¹) = 3 × 0.63212 = 1.896. One τ always covers 63.2 % of the journey, whatever the equation.', { tol: 0.005 }),
    q.num('E', 'A sinusoidal voltage has a peak value of 48 V. What is its RMS value, in volts (2 d.p.)?', 33.94,
      'For a sine, V_rms = peak/√2 = 48/1.41421 = 33.94 V. The √2 comes from the average of sin² over a cycle being ½, so it belongs to the sine and to no other waveshape.', { unit: 'V', tol: 0.03 }),
    q.num('E', 'A square wave alternates between +7 V and −7 V, spending equal time at each. What is its RMS value, in volts?', 7,
      'The square of the waveform is 49 V² at every instant, so the mean square is 49 and its root is 7 V. A square wave\'s RMS equals its peak; dividing by √2 here would be wrong by 29 %.', { unit: 'V' }),
    q.num('E', 'Exam-style: a sinusoidal voltage v(t) = 20·sin(ωt) volts is applied across a 25 Ω resistor. State the average power dissipated, in watts.', 8,
      'V_rms = 20/√2 = 14.14 V, so P = V_rms²/R = 200/25 = 8 W. Equivalently P = A²/(2R) = 400/50 = 8 W. The frequency never enters: a resistor does not care about it.', { unit: 'W', tol: 0.05 }),
    q.num('E', 'Exam-style: a voltage is v(t) = 5 + 12·sin(ωt) volts, a 12 V-peak sine sitting on a 5 V DC offset. State its RMS value in volts, to 2 decimal places.', 9.85,
      'Average the square: mean(25) + mean(120 sin ωt) + mean(144 sin²ωt) = 25 + 0 + 72 = 97 V². The cross term averages to zero over a whole cycle. V_rms = √97 = 9.85 V — not 5 + 8.49, because RMS contributions add in quadrature, not directly.', { unit: 'V', tol: 0.03 }),
    q.mc('E', 'While applying superposition to a linear circuit, you need to turn off a **current** source. What do you replace it with?', ['An open circuit (a gap)', 'A short circuit (a plain wire)', 'A resistor of zero ohms', 'A voltage source of the same value'], 0,
      'A current source fixes the current through itself whatever voltage appears, so fixing that current at 0 A for any voltage is exactly what a gap does. A dead **voltage** source is the opposite case: it forces 0 V for any current, which is a wire. Getting the two backwards changes the resistor network, and every number after it.'),
    q.num('E', 'Exam-style: a 12 V source drives node A through a 6 Ω resistor, a 3 Ω resistor runs from A to ground, and a 2 A current source pushes current into A. Using superposition, state the partial voltage at A due to the **12 V source acting alone**, in volts.', 4,
      'Turn the current source off, which makes it an open circuit, and what is left is a plain divider: V = 12 × 3/(6 + 3) = 12 × 1/3 = 4 V.', { unit: 'V', tol: 0.05 }),
    q.num('E', 'Exam-style: a 12 V source drives node A through a 6 Ω resistor, a 3 Ω resistor runs from A to ground, and a 2 A current source pushes current into A. State the total voltage at node A, in volts.', 8,
      'Two partial answers, then add. Voltage source alone (current source becomes an open circuit): a divider, 12 × 3/(6 + 3) = 4 V. Current source alone (voltage source becomes a wire): its 6 Ω now sits from A to ground in parallel with the 3 Ω, giving 6 × 3/9 = 2 Ω, and 2 A through 2 Ω is 4 V. Total 4 + 4 = 8 V. Check with a current balance at A: (12 − 8)/6 = 0.667 A in, plus 2 A in, equals 8/3 = 2.667 A out.', { unit: 'V', tol: 0.05 }),
    q.num('E', 'Exam-style: a 12 V source drives node A through a 6 Ω resistor, a 3 Ω resistor runs from A to ground, and a 2 A current source pushes current into A. State the power dissipated in the 3 Ω resistor, in watts (2 d.p.).', 21.33,
      'Superpose the voltages first: 4 V from each source gives 8 V at A. Then P = V²/R = 64/3 = 21.33 W. Adding the partial powers (4²/3 = 5.33 W twice, so 10.67 W) would be badly wrong: power is quadratic, and squaring a sum leaves a cross term that the partial powers do not contain.', { unit: 'W', tol: 0.1 }),
    q.mc('E', 'Interview: at a node in a linear circuit a signal source contributes 60 mV and an interference source contributes 8 mV. The interference source is made three times stronger while the signal source is unchanged. What is the new node voltage?', ['84 mV: the signal contribution stays at 60 mV and the interference contribution scales to 24 mV', '204 mV: both contributions triple', '68 mV: interference cannot change the total', '180 mV: tripling one source triples the total'], 0,
      'Linearity means each source scales its own contribution independently, and the contributions then add: 60 + 24 = 84 mV. Splitting a node voltage into a wanted part and an unwanted one is the practical reason to use superposition even when a direct solution would be quicker — it hands you the sensitivity to each source for free.'),
  ]
};
