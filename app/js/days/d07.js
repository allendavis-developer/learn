import { dayBuilder, W } from '../lib.js';
const q = dayBuilder(7);

// Day 7 is the week-1 checkpoint: no concept cards, no hints, no genius thread.
// Every question restates its own givens and uses values that appear nowhere in Days 1-6.
// See C:\dev\study\CLAUDE.md and app/CONTENT_GUIDE.md.
export default {
  title: 'Checkpoint 1 — the Week 1 diagnostic',
  emoji: '🏆',
  kind: 'checkpoint',
  strands: ['H', 'S', 'A', 'M', 'E'],
  summary: 'The handbook\'s week-1 diagnostic, covering Days 1–6 end to end. **Hardware**: unsigned and signed decoding, widths, hex, De Morgan, the mux, the full adder and the carry chain, sign extension and truncation, plus a bit-level subtractor to build. **Code**: indices and slices, aliasing and copies, `//` and `%`, `range`, dictionaries and sets, a loop invariant, and a deduplication program with a speed test. **Maths**: the dice grid, complements, "at least one", conditioning, distributions and expectation. **Degree**: charge, current, voltage, power and the passive sign convention, KCL and KVL, series and parallel, dividers and Thévenin, energy and efficiency, and derivatives as rates. **New values everywhere.** No hints, no retries; 80 % first-try passes. Record the result in your ledger without judging it against an invented cutoff.',
  takeaway: 'Checkpoint 1 done. Whatever I missed is now in the Ledger; Practice → Weak spots tomorrow morning before Day 8.',
  steps: [
    // =====================================================================================================
    // HARDWARE — Days 1, 2, 4, 5, 6
    // =====================================================================================================
    q.num('H', 'Decode the 8-bit pattern `10011101` as an **unsigned** number.', 157,
      'Set bits carry weights 128, 16, 8, 4 and 1: 128 + 16 + 8 + 4 + 1 = 157.'),
    q.num('H', 'Decode the 8-bit pattern `10011101` as a **signed** two\'s complement number.', -99,
      'In two\'s complement the top bit is worth −128, not +128: −128 + 16 + 8 + 4 + 1 = −99. Cross-check with the unsigned reading: 157 − 256 = −99.'),
    q.mc('H', 'What range of values can a **6-bit** signed two\'s complement register hold?', ['−32 to 31', '−31 to 32', '0 to 63', '−32 to 32'], 0,
      'Width w gives −2^(w−1) … 2^(w−1) − 1, so −2⁵ … 2⁵ − 1 = −32 … 31. The 32 patterns with the top bit clear cover 0 … 31 (zero uses one of them), and the 32 with it set cover −32 … −1.', { grid: true }),
    q.num('H', 'A counter must hold every whole number from 0 up to **700** inclusive. What is the minimum unsigned width, in bits?', 10,
      '2⁹ − 1 = 511, which is too small. 2¹⁰ − 1 = 1023 ≥ 700, so 10 bits. Size a register from the largest value it must hold, never from a round number.'),
    q.text('H', 'Encode the value **172** as an 8-bit unsigned pattern.', ['10101100'],
      'Greedy subtraction: 128 fits (44 left), 64 does not, 32 fits (12 left), 16 does not, 8 fits (4 left), 4 fits (0 left). Bits 7, 5, 3 and 2 are set: 10101100.', { mono: true, accept: ['^0*10101100$'] }),
    q.text('H', 'Encode **−45** as an 8-bit two\'s complement pattern.', ['11010011'],
      '256 − 45 = 211 = 128 + 64 + 16 + 2 + 1 = 11010011. Check by weights: −128 + 64 + 16 + 2 + 1 = −45. Invert-and-add-1 agrees: 45 is 00101101, inverted 11010010, plus 1 gives 11010011.', { mono: true, accept: ['^11010011$'] }),
    q.num('H', 'What does the byte `0xB4` represent as a **signed** 8-bit two\'s complement value?', -76,
      'The leading hex digit B is 8 or more, so the top bit is set and the value is negative. 0xB4 = 11 × 16 + 4 = 180 unsigned, so signed it is 180 − 256 = −76. By weights: 10110100 = −128 + 32 + 16 + 4 = −76.'),
    q.mc('H', 'By De Morgan\'s law, `NOT(A OR B)` is equal to which expression?', ['`(NOT A) AND (NOT B)`', '`(NOT A) OR (NOT B)`', '`A AND B`', '`A XOR B`'], 0,
      '"Neither of them" means "not this one **and** not that one". Push the NOT through the gate and the gate flips: OR becomes AND, and each input picks up its own NOT. Check A = 1, B = 0: the left side is NOT(1) = 0, and the right side is 0 AND 1 = 0.'),
    q.mc('H', 'A 2-to-1 multiplexer has data inputs A and B and select input S. With S = 1, A = 0 and B = 1, what is the output Y?', ['1, because S = 1 routes B to the output', '0, because S = 1 routes A to the output', '1, because Y is A OR B', '0, because Y is A AND B'], 0,
      'Y = (NOT S AND A) OR (S AND B). With S = 1 the first AND gate is held at 0 and the second passes B, so Y = B = 1. The value of A is still computed by the hardware and then discarded.'),
    q.mc('H', 'An unsigned 4-bit adder computes `1101 + 0101`. What appears at its outputs?', ['0010 with carry-out 1', '0010 with carry-out 0', '1110 with carry-out 0', '10010 in 4 bits'], 0,
      '13 + 5 = 18 = binary 10010, which needs five bits. The 4-bit result is 0010 (= 2) and the fifth bit leaves as the carry-out: 18 − 16 = 2, the wrap-around modulo 2⁴.'),
    q.mc('H', 'Sign-extend the 4-bit signed value `1100` (which is −4) to 8 bits. Which pattern results?', ['11111100', '00001100', '11000000', '10001100'], 0,
      'Replicate the sign bit into the four new positions. Check the value: −128 + 64 + 32 + 16 + 8 + 4 = −4, unchanged. Zero-extending to 00001100 would give +12 instead, destroying the negative value.', { grid: true }),
    q.mc('H', 'What are the inputs of a **full adder**?', ['A, B and a carry-in from the column to its right', 'A and B only', 'A, B and a select bit', 'A sum bit and a carry bit'], 0,
      'Three bits in, two bits out. A half adder takes only A and B, so it has nowhere to accept the carry arriving from the column on its right — which every column except the rightmost needs.'),
    q.num('H', 'Exam-style: a full adder in a ripple-carry adder has a propagation delay of 0.15 ns. What is the worst-case settling time of a **12-bit** ripple-carry adder, in nanoseconds?', 1.8,
      'In the worst case one carry has to cross every column in turn: 12 stages × 0.15 ns = 1.8 ns. The delay is proportional to the width, which is the whole objection to ripple carry — a 24-bit version would take 3.6 ns.', { unit: 'ns', tol: 0.02 }),
    q.num('H', 'Interview: a signed **10-bit** value is assigned to a signed **6-bit** port and the tool warns that the expression was truncated. For the truncation to change no value, how many of the leading bits of the 10-bit value must all be identical?', 5,
      'The four dropped bits plus the bit that becomes the new sign bit: bits 9 down to 5, so 5 bits. Equivalently the 10-bit value must be a sign-extension of a 6-bit one, i.e. it must lie in −32 … 31. Answering a truncation warning means giving that range argument, not silencing the warning.'),
    q.code('H', 'Build a two\'s complement subtractor. Write `solve(w, a, b)` where `a` and `b` are raw `w`-bit patterns given as non-negative integers. Compute `a - b` the way hardware does: invert every bit of `b`, then run a ripple chain of full adders over `a` and the inverted `b` with a carry-in of 1. Return `[difference, carry_out]`, where `difference` is the raw `w`-bit pattern as a non-negative integer and `carry_out` is the 0 or 1 leaving the top column. Do **not** use Python\'s `-` operator on `a` and `b`; model the gates.', {
      fn: 'solve',
      starter: 'def solve(w, a, b):\n    mask = (1 << w) - 1\n    a = a & mask\n    b = b & mask\n    carry = 1          # the +1 of invert-and-add-1 arrives as the carry-in of column 0\n    diff = 0\n    for i in range(w):\n        ai = (a >> i) & 1\n        bi = 1 - ((b >> i) & 1)      # the inverted bit of b\n        # sum bit = ai XOR bi XOR carry ; new carry = majority(ai, bi, carry)\n        pass\n    return [diff, carry]\n',
      tests: [
        { args: [8, 200, 45], expect: [155, 1], name: '200 − 45 = 155, no borrow' },
        { args: [8, 5, 5], expect: [0, 1], name: 'x − x = 0, and the carry ripples all the way out' },
        { args: [8, 0, 1], expect: [255, 0], name: '0 − 1 wraps to the all-ones pattern (−1); carry-out 0 means a borrow' },
        { args: [4, 9, 3], expect: [6, 1], name: '4-bit 9 − 3 = 6' },
        { args: [4, 3, 9], expect: [10, 0], name: '4-bit 3 − 9 = −6, stored as 16 − 6 = 10' },
        { args: [8, 128, 1], expect: [127, 1], name: 'the most negative byte minus 1' },
        { args: [12, 0, 0], expect: [0, 1], name: '0 − 0 = 0 at width 12' },
        { args: [1, 0, 1], expect: [1, 0], name: 'width 1: the only weight is −1' },
        { args: [1, 1, 1], expect: [0, 1], name: 'width 1: 1 − 1 = 0' },
      ],
      gen: 'def gen():\n    for _ in range(50):\n        w = random.randint(1, 16)\n        yield [w, random.randint(0, (1 << w) - 1), random.randint(0, (1 << w) - 1)]',
      refCode: 'def ref(w, a, b):\n    m = (1 << w) - 1\n    t = (a & m) + ((~b) & m) + 1\n    return [t & m, t >> w]',
      speed: { gen: 'def gen():\n    w = 2000\n    return [w, random.getrandbits(w), random.getrandbits(w)]', budgetMs: 1500, label: 'a 2000-bit subtraction' },
      solution: 'def solve(w, a, b):\n    mask = (1 << w) - 1\n    a = a & mask\n    b = b & mask\n    carry = 1\n    diff = 0\n    for i in range(w):\n        ai = (a >> i) & 1\n        bi = 1 - ((b >> i) & 1)\n        s = ai ^ bi ^ carry\n        carry = (ai & bi) | (ai & carry) | (bi & carry)\n        diff = diff | (s << i)\n    return [diff, carry]'
    }, 'a − b = a + (~b) + 1, because ~b + 1 is exactly −b: b + ~b is all-ones = −1, so b + (~b + 1) = 0. In gates that costs one inverter per bit and the adder you already have, with the +1 delivered free as the carry-in of column 0 — which is why a real ALU has a single "subtract" wire feeding both the inverters and that carry-in. Each pass is one full adder: XOR of the three bits for the sum, majority of the three for the carry going left. The carry-out has a meaning worth knowing: for unsigned operands, carry-out 1 means a ≥ b (no borrow) and 0 means the result wrapped.'),

    // =====================================================================================================
    // CODE — Days 1, 2, 3, 5
    // =====================================================================================================
    q.num('S', 'For `xs = [11, 22, 33, 44, 55]`, what does `xs[-3]` evaluate to?', 33,
      'A negative index counts from the end: xs[−3] means xs[len(xs) − 3] = xs[2], which is 33.'),
    q.num('S', 'For `xs = [11, 22, 33, 44, 55]`, what does `len(xs[1:4])` evaluate to?', 3,
      'A slice is half-open: it includes index 1 and stops before index 4, giving [22, 33, 44]. The length is simply 4 − 1 = 3, which is exactly why the stop is excluded.'),
    q.mc('S', 'After `a = [4]`, then `b = a`, then `b.append(7)`, what does `a` contain?', ['[4, 7]', '[4]', '[7]', '[[4], 7]'], 0,
      'b = a copies the arrow, not the list. Both names point at one object, so appending through b appends to the object a also names. Building a separate list needs a.copy(), list(a) or a[:].', { grid: true }),
    q.mc('S', 'After `a = [[1], [2]]`, then `c = a.copy()`, then `c[0].append(5)`, what does `a` contain?', ['[[1, 5], [2]]', '[[1], [2]]', '[[1, 5], [2, 5]]', 'an error'], 0,
      'copy() is shallow: it builds a new outer list whose slots hold the same two inner lists. Changing an inner list is visible through both outer lists. copy.deepcopy(a) would rebuild every level.'),
    q.num('S', 'What does `17 // 5 + 17 % 5` evaluate to in Python?', 5,
      '17 // 5 = 3 (five fits three times) and 17 % 5 = 2 (what is left), so 3 + 2 = 5. The identity behind both: 17 == 5 × 3 + 2.'),
    q.num('S', 'What does `-17 % 5` evaluate to in Python?', 3,
      'Python rounds the quotient down: −17 // 5 = −4. The remainder then has to satisfy 5 × (−4) + r = −17, so r = 3. For a positive divisor Python\'s remainder is always in 0 … b − 1, which is exactly the wrap-around a counter performs.'),
    q.tokens('S', 'What does `range(2, 11, 3)` produce, in order?', ['2', '5', '8'], ['11', '14'], 'Start at 2 and step by 3: 2, 5, 8. The next value would be 11, which is excluded because the stop is not included.', { mono: true }),
    q.mc('S', 'For `d = {"a": 3}`, what does `d.get("b", -2)` return?', ['−2', 'KeyError', 'None', '3'], 0,
      'The key "b" is missing, so .get hands back the default. Writing d["b"] instead would raise KeyError, which is the right behaviour when a missing key means a bug rather than a normal case.', { grid: true }),
    q.num('S', 'What does `len(set([4, 4, 7, 9, 9, 9]))` evaluate to?', 3,
      'A set holds each value once: {4, 7, 9}. A repeat hashes to a slot that already holds an equal value, and there is nowhere else for it to go.'),
    q.multi('S', 'Which of these values can be used as a **dictionary key** in Python?', ['the integer 12', 'the string "ab"', 'the tuple (1, 2)', 'the list [1, 2]'], [0, 1, 2],
      'A key must be immutable, because the slot the entry lives in is computed from the key\'s hash. If the key could change, its hash would change and the entry would be stranded in the wrong slot. A list is mutable, so Python raises TypeError: unhashable type.'),
    q.num('S', 'Trace this loop by hand: `value = 0`, then `for ch in "10110": value = value * 2 + (1 if ch == "1" else 0)`. What is `value` at the end?', 22,
      'The running value goes 0 → 1 → 2 → 5 → 11 → 22. Doubling shifts every bit already read one place left, then the new bit lands at weight 1. Check: 10110 = 16 + 4 + 2 = 22.'),
    q.num('A', 'A one-pass loop finds the largest value in a list of **30** numbers by adopting the first element and comparing each of the rest against the best so far. How many comparisons between elements does it make?', 29,
      'n − 1 = 29. It is also the best any algorithm can do: every element except the winner must lose at least one comparison, and each comparison produces exactly one loser.'),
    q.mc('A', 'What is a loop invariant?', ['A statement about the variables that is true before the loop and stays true after every pass, so at the exit it gives you the answer', 'The variable the loop counts with', 'A bug that repeats on every pass', 'The number of times the loop runs'], 0,
      'It is a proof in three parts: establishment (true before the first pass), maintenance (one pass preserves it) and termination (the invariant plus the reason the loop stopped is the result). A comment says what you hope; an invariant is something you can check line by line.'),
    q.mc('A', 'Interview: a function removes duplicates from a list of n items by keeping the values it has already seen in a **list** and testing `if x in seen` for each item. What is its running time, and what is the fix?', ['O(n²), because `in` on a list scans it from the front; make `seen` a set and each test becomes one hash and one jump, giving O(n)', 'O(n), because `in` is constant time on any collection', 'O(n log n), because Python sorts the list first', 'O(n²), and nothing can be done about it'], 0,
      'At n = 50,000 the list version does about 1.25 billion comparisons and the set version does 50,000 cheap steps. One word changes, the output does not. This is the first place where the data structure, not the loop, decides whether a program is usable.'),
    q.code('A', 'Write `solve(xs)`, a deduplicator. Given a list of integers, return `[unique, dropped]` where `unique` is a new list holding each distinct value **once, in order of first appearance**, and `dropped` is how many items were removed. `solve([3, 1, 3, 2, 1])` gives `[[3, 1, 2], 2]`, and `solve([])` gives `[[], 0]`. A 50,000-item list must finish inside the time budget, so remember what a membership test costs on a list.', {
      fn: 'solve',
      starter: 'def solve(xs):\n    seen = set()\n    unique = []\n    for x in xs:\n        # if x has not been seen, record it and keep it\n        pass\n    return [unique, len(xs) - len(unique)]\n',
      tests: [
        { args: [[3, 1, 3, 2, 1]], expect: [[3, 1, 2], 2] },
        { args: [[]], expect: [[], 0], name: 'empty input: an empty list and nothing dropped' },
        { args: [[9]], expect: [[9], 0], name: 'a single item' },
        { args: [[5, 5, 5, 5]], expect: [[5], 3], name: 'every item the same' },
        { args: [[1, 2, 3]], expect: [[1, 2, 3], 0], name: 'no repeats at all' },
        { args: [[0, 0, 7]], expect: [[0, 7], 1], name: 'zero is a perfectly good value' },
        { args: [[-2, 4, -2, 4, -2]], expect: [[-2, 4], 3], name: 'negatives, and order of first appearance is kept' },
      ],
      gen: 'def gen():\n    for _ in range(40):\n        n = random.randint(0, 25)\n        yield [[random.randint(0, 6) for _ in range(n)]]',
      refCode: 'def ref(xs):\n    seen = set()\n    out = []\n    for x in xs:\n        if x not in seen:\n            seen.add(x)\n            out.append(x)\n    return [out, len(xs) - len(out)]',
      speed: { gen: 'def gen():\n    return [[random.randint(0, 999) for _ in range(50000)]]', budgetMs: 1500, label: '50,000 items' },
      solution: 'def solve(xs):\n    seen = set()\n    unique = []\n    for x in xs:\n        if x not in seen:\n            seen.add(x)\n            unique.append(x)\n    return [unique, len(xs) - len(unique)]'
    }, 'The invariant: after k items, `unique` holds the distinct values among the first k in order of first appearance, and `seen` holds exactly those values. It is established by the empty list before the loop (no items, no values) and maintained by the one branch inside. The set is what keeps it linear — `x not in seen` is one hash and one jump however many values are already stored, while the same test on a list is a scan that gets longer as the list grows. Counting `dropped` as `len(xs) - len(unique)` rather than with a counter is the same fact stated once instead of twice, so the two can never disagree.'),

    // =====================================================================================================
    // MATHS — Days 1, 2, 3, 4, 5, 6
    // =====================================================================================================
    q.num('M', 'Two fair six-sided dice are rolled. What is P(the sum is 9)?', 4 / 36,
      'The 36 ordered pairs are equally likely. A sum of 9 comes from (3,6), (4,5), (5,4) and (6,3): 4 cells, so 4/36 = 1/9 ≈ 0.111.', { display: '4/36 = 1/9' }),
    q.num('M', 'Two fair six-sided dice are rolled. What is P(the sum is 5 **or** the sum is 10)?', 7 / 36,
      'Sum 5: (1,4), (2,3), (3,2), (4,1) — 4 cells. Sum 10: (4,6), (5,5), (6,4) — 3 cells. No cell can have both sums, so the counts simply add: 7/36 ≈ 0.194.', { display: '7/36' }),
    q.num('M', 'Two fair six-sided dice are rolled. What is P(the sum is **not** 9)?', 32 / 36,
      'Complement: P(not A) = 1 − P(A). A sum of 9 has 4 of the 36 cells, so the other 32 remain: 32/36 = 8/9 ≈ 0.889.', { display: '32/36 = 8/9' }),
    q.num('M', 'Two fair six-sided dice are rolled. What is P(at least one die shows a 3)?', 11 / 36,
      'Count what you do not want: neither die shows a 3 leaves 5 faces each, so 5 × 5 = 25 cells. 1 − 25/36 = 11/36 ≈ 0.306. Counting directly agrees: 6 cells with a 3 first, 6 with a 3 second, minus the shared (3,3).', { display: '11/36' }),
    q.num('M', 'Exam-style: two fair six-sided dice are rolled. What is P(exactly one of the two dice shows a 3)?', 10 / 36,
      'Split the outcomes into pieces that cannot overlap: no 3 (5 × 5 = 25 cells), exactly one 3 (a 3 on the first die with a non-3 on the second, 1 × 5 = 5 cells, or the other way round, another 5) and two 3s (the single cell (3,3)). Check: 25 + 10 + 1 = 36. So P(exactly one) = 10/36 = 5/18 ≈ 0.278.', { display: '10/36 = 5/18' }),
    q.num('M', 'Two fair six-sided dice are rolled and you are told the sum is **even**. What is P(both dice show an even number)?', 0.5,
      'Shrink the sample space: an even sum needs both dice even (3 × 3 = 9 cells) or both odd (another 9), so 18 outcomes survive. Nine of them have both dice even: 9/18 = ½. Before the news it was 9/36 = ¼ — the dice did not change, the pool of possibilities did.', { display: '9/18 = 1/2' }),
    q.num('M', 'A fair coin is flipped 5 times. What is P(exactly one head)?', 5 / 32,
      'There are 2⁵ = 32 equally likely sequences. Exactly one head means choosing which of the 5 positions holds it: 5 sequences. So 5/32 = 0.15625.', { display: '5/32' }),
    q.num('M', 'A fair coin is flipped 9 times. What is the expected number of heads?', 4.5,
      'Put an indicator on each flip: it is 1 for a head, and its expectation is ½. Expectations add whatever the flips do, so 9 × ½ = 4.5. No listing of 512 sequences is needed, and 4.5 is not a number of heads you can ever see.'),
    q.num('M', 'Four fair six-sided dice are rolled. What is the expected value of their total?', 14,
      'One die averages (1+2+3+4+5+6)/6 = 3.5, and E[X + Y] = E[X] + E[Y] always: 4 × 3.5 = 14.'),
    q.num('M', 'A fair six-sided die is rolled 24 times. What is the expected number of rolls that show a 1?', 4,
      'An indicator per roll, each with probability 1/6: 24 × 1/6 = 4. The actual count scatters around 4; the expectation is its centre, not a guarantee.'),
    q.num('M', 'A fair six-sided die is rolled once. You win £10 if it shows a 6 and lose £2 otherwise. What is your expected profit, in pounds?', 0,
      'Work from the payout column, not the face column: +10 with probability 1/6, and −2 with probability 5/6. E = (10 − 10)/6 = £0. The game is exactly fair, which no single roll will ever show you.', { unit: '£' }),
    q.tf('M', 'The rule E[X + Y] = E[X] + E[Y] holds only when X and Y are independent.', false,
      'It holds always. Adding expectations is a rearrangement of a finite sum — each outcome contributes its x and its y once on both sides — so dependence cannot break it. That is precisely why linearity solves problems where independence fails.'),
    q.mc('M', 'A red die and a blue die are rolled. Are the events "the sum is 7" and "the blue die shows a 4" independent?', ['Yes: P(sum 7) = 1/6, P(blue is 4) = 1/6, and P(both) = 1/36, which is the product', 'No: the sum obviously depends on the blue die', 'Yes, because the two dice are physically separate objects', 'No, because they can both happen at once'], 0,
      'Both together is the single cell (3, 4), so 1/36, and (1/6)(1/6) = 1/36 as well. Whatever the blue die shows, exactly one red value completes a 7, so the blue die carries no information about a seven. Independence is a numerical test, not a story: change the target sum to 4 and it fails.'),
    q.num('M', 'Interview: a family has two children, each equally likely to be a boy or a girl, independently. Given that at least one of the two is a girl, what is the probability that both are girls?', 1 / 3,
      'The four equally likely outcomes are GG, GB, BG, BB. "At least one girl" rules out BB and leaves three, still equally likely, exactly one of which is GG: 1/3. The instinct to say ½ comes from silently reading the condition as "the elder is a girl", which would leave only GG and GB.', { display: '1/3 ≈ 0.333', tol: 0.005 }),
    q.num('M', 'A bag holds 4 red and 6 blue counters. Two are drawn without replacement. What is P(both are red)?', 2 / 15,
      'First red: 4/10. Given that, 3 reds remain among 9 counters: 3/9. Multiply along the branch (the general rule P(A and B) = P(A)·P(B | A), which needs no independence): (4/10)(3/9) = 12/90 = 2/15 ≈ 0.133. With replacement it would be (4/10)² = 0.16.', { display: '2/15 ≈ 0.133', tol: 0.003 }),
    q.num('M', 'Interview: a communications link drops each packet independently with probability 0.05. A message is made of 4 packets and is useless unless every one of them arrives. What is the probability that the message fails, to 4 decimal places?', 1 - Math.pow(0.95, 4),
      'Go through the complement. All four arrive with probability 0.95⁴ = 0.8145, because the packets are independent. So the message fails with probability 1 − 0.8145 = 0.1855. The tempting 4 × 0.05 = 0.20 double-counts the cases where more than one packet is lost, and with 25 packets it would exceed 1.', { display: '1 − 0.95⁴ ≈ 0.1855', tol: 0.0008 }),
    q.num('M', 'Interview: a fair coin is flipped 12 times, so there are 11 adjacent pairs of consecutive flips. What is the expected number of adjacent pairs whose two flips match (HH or TT)?', 5.5,
      'Put an indicator on each of the 11 adjacent pairs: it is 1 when the pair matches, which happens with probability ½ (whatever the first flip is, the second matches it half the time). Expectations add, so the answer is 11 × ½ = 5.5. The pairs overlap and are certainly not independent, and linearity does not care.'),
    q.num('M', 'Interview: a fair **four**-sided die shows 1, 2, 3 or 4, each with probability ¼. What is E[X²]?', 7.5,
      '(1 + 4 + 9 + 16)/4 = 30/4 = 7.5. Compare E[X]² = 2.5² = 6.25: averaging then squaring is a different operation from squaring then averaging, and the 1.25 gap is exactly the spread of the distribution.'),

    // =====================================================================================================
    // DEGREE — Days 1, 2, 3, 4, 5, 6
    // =====================================================================================================
    q.num('E', 'A voltage of 18 V is applied across a 4 Ω resistor. What current flows through it, in amperes?', 4.5,
      'Ohm\'s law: I = V/R = 18/4 = 4.5 A.', { unit: 'A' }),
    q.num('E', 'A voltage of 18 V is applied across a 4 Ω resistor, driving 4.5 A through it. How much power does the resistor dissipate, in watts?', 81,
      'P = V·I = 18 × 4.5 = 81 W. The other two forms agree: I²R = 20.25 × 4 = 81 and V²/R = 324/4 = 81. A part dissipating 81 W needs serious heatsinking — a common ¼ W resistor would be destroyed instantly.', { unit: 'W' }),
    q.num('E', 'A steady current of 0.4 A flows past a point in a wire for 25 seconds. How much charge passes, in coulombs?', 10,
      'Current is charge per second, I = Q/t, so Q = I·t = 0.4 × 25 = 10 C.', { unit: 'C' }),
    q.num('E', 'A component has 8 V across it with the **+** label at its left end, and 3 A flows **out of** its left end. Under the passive sign convention, what is its power in watts, sign included?', -24,
      'The convention says take the current entering the + end. Here 3 A leaves that end, so the current entering it is −3 A: P = 8 × (−3) = −24 W. Negative power means the component **delivers** 24 W, so it is acting as a source.', { unit: 'W' }),
    q.num('E', 'Three resistors of 150 Ω, 250 Ω and 400 Ω are connected in **series**. What is the total resistance, in ohms?', 800,
      'One path means one common current (KCL at each joint), so the voltages add and the resistances add: 150 + 250 + 400 = 800 Ω.', { unit: 'Ω' }),
    q.num('E', 'A 200 Ω resistor and a 600 Ω resistor are connected in **parallel**. What is the total resistance, in ohms?', 150,
      'Conductances add: 1/R = 1/200 + 1/600 = 3/600 + 1/600 = 4/600, so R = 150 Ω. Product over sum agrees: 200 × 600 / 800 = 150. It must be below 200 Ω, because an extra lane cannot slow the traffic down.', { unit: 'Ω' }),
    q.num('E', 'Exam-style: a single loop contains a 30 V source in series with resistors of 5 Ω, 7 Ω and 3 Ω. Calculate the loop current, in amperes.', 2,
      'KVL round the loop: the 30 V rise is cancelled by the three drops, 30 = 5I + 7I + 3I = 15I, so I = 2 A. One path means the same current flows in all three resistors.', { unit: 'A' }),
    q.num('E', 'Exam-style: a single loop contains a 30 V source in series with resistors of 5 Ω, 7 Ω and 3 Ω, carrying 2 A. Calculate the voltage across the **7 Ω** resistor, in volts.', 14,
      'V = I·R = 2 × 7 = 14 V. Check with KVL: 10 V + 14 V + 6 V = 30 V, the whole source voltage.', { unit: 'V' }),
    q.num('E', 'Exam-style: a single loop contains a 30 V source in series with resistors of 5 Ω, 7 Ω and 3 Ω, carrying 2 A. Calculate the power dissipated in the **3 Ω** resistor, in watts.', 12,
      'P = I²R = 2² × 3 = 12 W. (Or 6 V across it × 2 A = 12 W.) The source delivers 30 × 2 = 60 W, and 20 + 28 + 12 = 60 W is absorbed — conservation of energy, checked.', { unit: 'W' }),
    q.num('E', 'At a node, 5 A and 4 A flow **in** along two wires and 3 A flows **out** along a third. A fourth wire is the only other connection. How many amperes flow **out** along that fourth wire?', 6,
      'Kirchhoff\'s current law is conservation of charge at a point: a junction has nowhere to store charge. In = out, so 5 + 4 = 3 + x and x = 6 A.', { unit: 'A' }),
    q.num('E', 'Exam-style: a 20 V supply drives R1 = 6 kΩ in series with R2 = 4 kΩ, with the output taken across R2 and nothing connected to it. Calculate the output voltage, in volts.', 8,
      'One current flows in both resistors: I = 20/10,000 = 2 mA. Vout = I·R2 = 0.002 × 4000 = 8 V. As a ratio: R2 takes 4/10 of the total resistance, so it takes 4/10 of the supply.', { unit: 'V' }),
    q.num('E', 'Exam-style: a 20 V supply drives R1 = 6 kΩ in series with R2 = 4 kΩ, with the output taken across R2. Calculate the Thévenin resistance seen looking back into that output, in ohms.', 2400,
      'Switch the ideal source off, which means replacing it by a short circuit (a 0 V source holds 0 V at any current — that is a wire). R1 then runs from the output to ground alongside R2: 6k ∥ 4k = 24/10 kΩ = 2400 Ω.', { unit: 'Ω', tol: 10 }),
    q.num('E', 'Exam-style: a 20 V supply drives R1 = 6 kΩ in series with R2 = 4 kΩ, and a 6 kΩ load is then connected across R2. Calculate the output voltage, in volts, to 2 decimal places.', 5.71,
      'Use the Thévenin equivalent: 8 V behind 2.4 kΩ. The load makes a new divider with that resistance: V = 8 × 6000/(6000 + 2400) = 48/8.4 = 5.71 V. The unloaded 8 V was a promise the divider could not keep, because the load current has to flow through Rth.', { unit: 'V', tol: 0.03 }),
    q.num('E', 'A 900 W appliance runs for 5 hours. How much energy does it use, in **kilowatt-hours**?', 4.5,
      'E = P × t = 0.9 kW × 5 h = 4.5 kWh. In joules that is 900 × 18,000 = 16.2 MJ, and 4.5 × 3.6 MJ = 16.2 MJ agrees.', { unit: 'kWh', tol: 0.02 }),
    q.num('E', 'Exam-style: a motor draws 750 W from the supply and delivers 600 W of mechanical power. Calculate its efficiency, as a **percentage**.', 80,
      'η = P_out / P_in = 600/750 = 0.80, so 80 %. The missing 150 W is dissipated as heat in the windings and bearings — under the passive sign convention it is simply absorbed by a component you were not thinking about.', { unit: '%', tol: 0.5 }),
    q.num('E', 'Exam-style: the charge that has passed a point in a circuit is q(t) = 2t³ + 5t coulombs, with t in seconds. Calculate the current at t = 3 s, in amperes.', 59,
      'Current is the rate of flow of charge: i = dq/dt = 6t² + 5 by the power rule. At t = 3 s that is 6 × 9 + 5 = 59 A.', { unit: 'A' }),
    q.num('E', 'Exam-style: the voltage across a 4 µF capacitor is v(t) = 3t² volts, with t in seconds. Calculate the current into the capacitor at t = 5 s, in **microamps**.', 120,
      'A capacitor obeys i = C·dv/dt. Here dv/dt = 6t, which is 30 V/s at t = 5 s, so i = 4 × 10⁻⁶ × 30 = 1.2 × 10⁻⁴ A = 120 µA. No change of voltage would mean no current at all.', { unit: 'µA', tol: 1 }),
    q.num('E', 'For f(x) = x² − 6x + 5, at what value of x is the curve momentarily flat (that is, f′(x) = 0)?', 3,
      'The power rule gives f′(x) = 2x − 6, which is zero at x = 3. The slope runs from negative to positive through that point, so the curve falls then rises: it is a minimum, and f(3) = 9 − 18 + 5 = −4.'),
  ]
};
